---
generated-by: refine
generated-date: 2026-06-30
last-refined: 2026-06-30
project-types: [dotnet, docker/podman, protobuf-grpc]
source-registry: "~/.agents/docs/projects/README.md"
---

# Project Specialists

Customize which review specialists run on this project. Generated by `/refine` - manual edits preserved in `## Custom` and `## Disabled`.

## Type-Activated

Specialists activated by detected project type. From registry lookup.

- +protobuf - `dotnet` project type includes protobuf review; repo also contains `src/PNet.Mesh/Protos/MeshProtocol.proto`.

## Cross-Cutting

Specialists activated via reasoning pass - signals found in project files, deps, or code patterns.

- +container - `src/PNet.Mesh.TestNode/Dockerfile` and Testcontainers-backed mesh flows.
- +config - `src/PNet.Mesh.TestNode/appsettings.json` and Testcontainers runtime configuration.

## Custom

Project-local specialist roles (from `.agents/codex/roles/team-review-*.md`).

- None detected.

## Disabled

Specialists explicitly excluded for this project. Manual entries preserved across regeneration.

## Project Domains

Custom scope tags the classifier uses alongside standard scopes. Derived from top-level `src/` directories or manually defined.

- PNet.Mesh
